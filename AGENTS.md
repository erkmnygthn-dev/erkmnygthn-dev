# AGENTS.md

## Cursor Cloud specific instructions

This is a **GitHub profile README repository** (`erkmnygthn-dev/erkmnygthn-dev`). The repo contains only a `README.md` that renders on the owner's GitHub profile page.

### Key facts

- **No runnable application**: There is no server, frontend, CLI, or any executable code.
- **No dependencies**: No `package.json`, `requirements.txt`, `Gemfile`, `go.mod`, or any dependency file exists.
- **No tests or lint**: There are no test frameworks, linters, or CI pipelines configured.
- **No build system**: No compilation, bundling, or build steps are needed.

### Development workflow

Editing this repo means editing `README.md` (GitHub-flavored Markdown). To preview changes, use any Markdown previewer or push to GitHub and view the profile page.
